<%= render 'user_profile_settings/header' %>

<div class="user-profile-settings">
  <div class="row">
    <div class="side-bar col-lg-3">
      <%= render partial: 'users/user_profile_nav', locals: {current_index: 'HEADERS'}  %>
    </div>
    <div class="main-content col-lg-9">
      <div class="row page-header">
        <div class="col-sm-6">
          <label class="title">
            HEADERS
          </label>
        </div>
        <div class="col-sm-6 text-right">
          <%= link_to 'HEADERS', usr_headers_path, class: 'header-btn headers-link-btn' %>
          <%= link_to 'SKINS', usr_skins_path, class: 'header-btn skins-link-btn' %>
        </div>
      </div>
      <div class="row page-content headers-skins-content">
        <div class="col-sm-12">
          <div class="row">
            <div class="col-sm-12">
              <div class="total-numbers">2018 | <span class="real-num">6/12</span> GIFTED OR ARCHIEVED</div>
            </div>
          </div>
          <div class="row">
            <% @headers.each do |header| %>
              <div class="col-sm-6">
                <%=form_for header, url: update_user_header_path, html: {method: 'put'} do |f| %>
                  <%= f.hidden_field :id %>
                  <label class="header-label"><%= header.description %>  </label>
                  <div class="header-content">
                    <%= image_tag header.url, class: 'header-img' %>
                    <% if current_user.header_id == header.id %>
                      <button class="current">
                        CURRENT HEADER
                      </button>
                    <% else %>
                      <%= f.submit "APPLY HEADER", class:"apply" %>
                    <% end %>
                  </div>
                <% end %>
              </div>
            <% end %>
<!--             <div class="col-sm-6">
              <label class="header-label">VIB Header   </label>
              <div class="header-content">
                <%= image_tag 'user_profile_settings/header_2.png'%>
                <button class="apply">
                  APPLY HEADER
                </button>
              </div>
            </div>
            <div class="col-sm-6">
              <label class="header-label">Claude Vonstroke Gift header  </label>
              <div class="header-content">
                <%= image_tag 'user_profile_settings/header_3.png'%>
                <button class="apply">
                  APPLY HEADER
                </button>
              </div>
            </div>
            <div class="col-sm-6">
              <label class="header-label">Claude Vonstroke Gift header  </label>
              <div class="header-content">
                <%= image_tag 'user_profile_settings/header_4.png'%>
                <button class="apply">
                  APPLY HEADER
                </button>
              </div>
            </div>
            <div class="col-sm-6">
              <label class="header-label">Dirtybird BBQ Oakland Header  </label>
              <div class="header-content">
                <%= image_tag 'user_profile_settings/header_5.png'%>
                <button class="apply">
                  APPLY HEADER
                </button>
              </div>
            </div>
            <div class="col-sm-6">
              <label class="header-label">Dirtybird Campout VIB Header  </label>
              <div class="header-content">
                <%= image_tag 'user_profile_settings/header_6.png'%>
                <button class="apply">
                  APPLY HEADER
                </button>
              </div>
            </div> -->
          </div>
        </div>
      </div>
    </div>
  </div>
</div>